# データ保存先

## SQLite データベース
- パス: `%USERPROFILE%\Documents\kurabete-notebook\library.db`
- 起動時に自動作成される。
- テーブル: `videos`, `cases`, `case_fields`

## ファイル管理
- 動画ファイルはオリジナルの場所で参照されます（コピーされません）。
- DBファイルのみが `Documents\kurabete-notebook\` に保存されます。
